[tool.poetry]
name = "build"
version = "0.1.0"
description = ""
authors = ["k2ntum <khainguyen@umass.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
typer = "^0.9.0"
rich = "^13.7.0"
pyinstaller = {version = "^6.3.0", python = "3.11"}

[tool.poetry.scripts]
build = "build.app:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
